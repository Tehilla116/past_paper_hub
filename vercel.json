{
  "version": 2,
  "buildCommand": "npm install --prefix client && npm run build:client",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}